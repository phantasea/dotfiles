[user]
	name = phantasea
	email = phantasea@gmail.com

[color]
    ui = true

[core]
    pager = less
    editor = vim
    quotepath = false
    mergeoptions = --no-edit
    autocrlf = input
    safecrlf = false
    excludesfile = /opt/.etc/gitignore

[diff]
    tool = vimdiff

[difftool]
    prompt = false

[diff "word"]
	textconv = strings

[diff "exif"]
	textconv = exiftool

[merge]
    keepBackup = false
    tool = vimdiff
    conflictstyle = diff3

[mergetool]
    prompt = false

[push]
	default = matching

[svn]
    rmdir = true

[alias]
    a  = add
    c  = clone
    d  = diff
    co = checkout
    ci = commit -m
    br = branch
    st = status
    lg = log
    dt = difftool
    mt = mergetool
    rv = remote -v
    last = log -1 HEAD
    unstage = reset HEAD --
    lg = "log --color --graph --abbrev-commit --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'"

